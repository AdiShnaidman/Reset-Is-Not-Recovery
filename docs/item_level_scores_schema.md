# Item-Level Scores Schema

Primary source schemas are documented by artifact path and checksum in `manifests/item_manifest_summary.csv`.

Key source files include hierarchy raw rows, pressure-ramp item summaries, truncation summaries, CCTP item-level rows, and semantic relabel aggregate summaries.
